streamlit>=1.26.0
pandas>=2.0.0
numpy>=1.25.0
matplotlib>=3.7.0
fpdf>=1.7.2
Pillow>=10.0.0

# Optional for OCR and PDF digitization
pytesseract>=0.3.10
pdf2image>=1.16.3

# System requirement for pdf2image
# poppler-utils (Linux) or poppler (Mac) must be installed separately